services:
  mysql:
    image: mysql:8.0
    container_name: laravel_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: authuser
    ports:
      - "3307:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
